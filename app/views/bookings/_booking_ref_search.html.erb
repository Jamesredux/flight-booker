<div class="container search">

<%= form_tag(bookings_show_path, method: "get") do %>
		
		<div class="row search_booking">
		

<div class="col-md-3 ">
				
				<%= label_tag(:id, "Booking Ref") %>
				<%= text_field_tag(:id) %>
				<%= submit_tag("Search") %>
			</div>
		</div>
		
			
			
	
	<% end %>
</div>